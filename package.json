{
  "name": "ml-copilot",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "node --experimental-strip-types scripts/build-check.ts",
    "typecheck": "node --experimental-strip-types scripts/typecheck-note.ts",
    "test": "node --experimental-strip-types --test \"src/**/*.test.ts\"",
    "phase0:probe": "node --experimental-strip-types scripts/phase0-probe.ts"
  }
}
